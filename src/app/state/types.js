const feature = '[retro]';

export const ADD_SESSION = `${feature} add session`;
export const SET_CURRENT_SESSION = `${feature} set current session`;

export const REQUEST_CATEGORIES = `${feature} request categories`;
