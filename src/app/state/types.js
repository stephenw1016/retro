const feature = '[retro]';

export const SAVE_SESSION_REQUEST = `${feature} save session request`;
export const SAVE_SESSION_SUCCESS = `${feature} save session success`;
export const SET_CURRENT_SESSION = `${feature} set current session`;

export const REQUEST_CATEGORIES = `${feature} request categories`;
